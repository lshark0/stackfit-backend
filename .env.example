PORT=4000
# 토큰 서명용 비밀키. 운영 배포 전 반드시 무작위 값으로 교체하세요.
JWT_SECRET=stackfit-dev-secret-change-me

# 설정하면 PostgreSQL을 사용합니다 (영구 저장). 비워두면 로컬 SQLite로 자동 동작합니다 (로컬 개발용).
# DATABASE_URL=postgresql://user:password@host/dbname
