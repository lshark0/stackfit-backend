# STACKFIT 보안 취약점 감사 보고서 (개인정보 탈취 관점)

감사일: 2026-08-28
범위: 백엔드 전체 라우트, 인증/토큰 로직, DB 계층, 프론트엔드 렌더링

---

## 요약

| 심각도 | 항목 | 상태 |
|---|---|---|
| 높음 | 이력서 PDF 인증 없이 전체 공개 | ✅ 수정 완료 |
| 중간 | 구글 로그인 이메일 미검증 자동연동 | ✅ 수정 완료 |
| 낮음 | 비밀번호 변경/탈퇴 API 무차별 대입 방어 부족 | ✅ 수정 완료 |
| 낮음 | 채팅 시작 시 상대방 계정 검증 누락 | ✅ 수정 완료 |
| 정보 | 데모 계정 약한 비밀번호가 운영 DB에 존재 | ⚠️ 권고사항 (조치는 사용자 선택) |

**SQL 인젝션, XSS, 권한 우회(IDOR), 비밀번호 저장 방식은 전수 검토 결과 문제 없음.**

---

## 1. [높음] 이력서 PDF가 인증 없이 전체 공개됨

**문제**: `/uploads/*` 경로가 `express.static`으로 그대로 서빙되어, 로그인하지 않은 사람도 URL만 알면
이력서 PDF(이름·경력·연락처 등 개인정보 포함)를 다운로드할 수 있었습니다. 파일명이 24자리 랜덤
문자열이라 무작위 추측은 사실상 불가능하지만(96비트 엔트로피), URL이 한 번이라도 캡처화면·로그·
공유링크 등으로 유출되면 영구적으로 아무나 접근 가능한 상태였습니다.

**수정**: 로그인(유효한 토큰)한 사용자만 접근 가능하도록 변경했습니다.

---

## 2. [중간] 구글 로그인 시 이메일 미검증 상태로 계정 자동연동

**문제**: 구글 로그인 시 응답에 포함된 `email_verified`(이메일 인증 여부) 값을 확인하지 않고
바로 그 이메일로 기존 계정에 자동 연동하고 있었습니다. 이론적으로 구글 계정의 이메일이
미인증 상태인 경우(극히 드문 케이스긴 하지만 G Suite/Workspace 도메인 등에서 발생 가능),
공격자가 피해자와 같은 이메일 주소를 미인증 상태로 등록한 구글 계정을 만들어 로그인하면
피해자의 기존 계정(채팅, 지원내역, 프로필 등)을 가로챌 수 있는 이론적 경로가 있었습니다.

**수정**: `email_verified`가 `true`인 경우에만 이메일 기반 자동연동을 수행하도록 변경했습니다.

---

## 3. [낮음] 비밀번호 변경/회원탈퇴 API의 무차별 대입 방어 부족

**문제**: 로그인·회원가입에는 15분당 20회 제한이 걸려있었지만, 이미 로그인된 상태에서
현재 비밀번호를 확인하는 "비밀번호 변경"과 "회원탈퇴" API는 전체 API 공용 제한(분당 120회)만
적용되어 있었습니다. 토큰이 탈취된 공격자가 짧은 비밀번호를 빠르게 대입 시도할 여지가 있었습니다.

**수정**: 두 엔드포인트에도 로그인과 동일한 강한 제한(15분당 20회)을 적용했습니다.

---

## 4. [낮음] 채팅 시작 시 상대방 계정 검증 누락

**문제**: 채팅을 시작할 때 상대방의 사용자 ID를 클라이언트가 보내는 값 그대로 사용하고 있어,
존재하지 않거나 역할이 다른(예: 프리랜서인데 companyId로 지정) 임의의 사용자 ID를 지정해
스팸성 대화/알림을 발생시킬 수 있는 여지가 있었습니다. 개인정보 유출은 아니지만 임의 사용자를
대상으로 한 알림 남용(스팸)이 가능한 인가 허점이라 함께 수정했습니다.

**수정**: 대화 시작 전 상대방이 실제로 해당 역할의 계정인지 검증하도록 변경했습니다.

---

## 5. [정보/권고] 데모 계정의 약한 비밀번호가 운영 DB에 남아있음

`lee@stackfit.dev`, `hr@hanbit.dev` 등 데모 계정이 `demo1234`라는 예측 가능한 비밀번호로
운영 DB에 실제로 존재합니다. 실제 개인정보는 아닌 테스트용 데이터지만, 로그인 화면에서
버튼은 제거했어도 계정 자체는 살아있어 이메일을 아는 사람은 로그인해 데모 데이터를 조회/조작할
수 있습니다. 필요하시면 이 계정들을 비활성화하거나 강한 비밀번호로 교체해드릴 수 있습니다.

---

## 검토했지만 문제 없었던 항목

- **SQL 인젝션**: 모든 쿼리가 `?` 파라미터 바인딩을 사용, 문자열 결합 없음 — 안전
- **XSS**: 사용자 입력이 화면에 표시되는 모든 지점에서 `esc()` 이스케이프 함수 일관 적용 확인 — 안전
- **비밀번호 저장**: scrypt + 사용자별 랜덤 salt + 타이밍 공격 방지 비교 — 안전
- **권한 우회(IDOR)**: 공고 삭제, 지원자 조회, 채팅, 프로젝트, 리뷰 등 모든 개인 리소스에
  소유권 확인 로직 존재 — 안전
- **비밀번호 무차별 대입**: 로그인 API 15분당 20회 제한 확인
- **의존성 취약점**: `npm audit` 결과 0건
- **에러로 인한 서버 다운**: 모든 라우트가 비동기 에러 핸들러로 감싸져 있어 예외 발생 시 서버가
  죽지 않고 500 응답만 반환 — 안전

---

## 구조적으로 알아두시면 좋은 점 (즉시 조치 불필요)

로그인 토큰을 브라우저 `localStorage`에 저장하고 있습니다. 이는 새로고침해도 로그인이 유지되게
하기 위한 표준적인 방식이지만, **만약 앱에 XSS 취약점이 생기면 토큰이 탈취될 수 있는 구조**입니다.
현재는 XSS 취약점이 발견되지 않았지만, 앞으로 새 기능을 추가할 때도 사용자 입력을 화면에 표시할
때는 반드시 `esc()`를 거치도록 유지해야 합니다.
